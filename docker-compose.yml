services:
  your-server-board:
    build: .
    container_name: your-server-board
    restart: unless-stopped
    ports:
      - "${YSB_PORT:-3050}:3000"
    volumes:
      - ./config:/app/config
    environment:
      - HOMEPAGE_ALLOWED_HOSTS=${YSB_ALLOWED_HOSTS}
